DEBUG = False                       # extra print statements
loop_interval = 1.5                 # sleep time between loops
json_interval = 60                  # interval for building a json object
post_interval = 1200                # interval for POSTing the array of json objects
low_power_multiplier = 1            # multiply all the above values when battery is low
use_temperature_buffers = False     # use if errant values need to be smoothed out
